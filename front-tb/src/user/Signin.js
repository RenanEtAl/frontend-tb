import React, { Component } from 'react'
import Layout from '../core/Layout'

const Signin =() => (

    <Layout title="Signin" description="Sign in to your account if you are already signed up.">
        ...
    </Layout>

    )

export default Signin